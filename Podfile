# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'cardio-spike' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'CardIO'

  # Pods for cardio-spike

  target 'cardio-spikeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'cardio-spikeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
